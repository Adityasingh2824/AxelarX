# AxelarX EVM Contracts Environment Configuration
# Copy this to .env and fill in your values
# WARNING: Never commit .env to version control!

# Wallet Configuration (derive private key from your mnemonic)
# Recovery phrase: possible obey else almost toward journey beyond avoid ecology learn decrease inside
# Use this command to get the private key:
# node -e "const { ethers } = require('ethers'); console.log(ethers.Wallet.fromPhrase('possible obey else almost toward journey beyond avoid ecology learn decrease inside').privateKey)"
PRIVATE_KEY=your_private_key_here

# Your wallet address
DEPLOYER_ADDRESS=0xCF2E0DBEde2B76d79c7D3bd5c6FD3eC4CD8BbeB8

# RPC & API Keys (optional for testnet)
ALCHEMY_API_KEY=
ETHERSCAN_API_KEY=
BASESCAN_API_KEY=

# Network to deploy to (baseSepolia, sepolia, or localhost)
NETWORK=baseSepolia








